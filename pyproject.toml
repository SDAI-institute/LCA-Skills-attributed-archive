[project]
name = "lca-skills"
version = "0.2.0"
description = "Portable Agent Skills and runtime adapters for expert life cycle assessment"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "SDAI Lab" }]
dependencies = []

[project.optional-dependencies]
openlca = ["olca-ipc", "olca-schema"]
