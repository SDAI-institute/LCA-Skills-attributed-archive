# Child-skill contract

Before invoking a child workflow, provide study path, plan/task IDs, intended output, allowed actions, relevant constraints, and return schema. After it returns, verify its artifacts and gate evidence before continuing.

When the host supports parallel subagents, parallelize independent research/review passes, not mutually dependent model edits. One practitioner/orchestrator owns the canonical model and merges accepted changes.
